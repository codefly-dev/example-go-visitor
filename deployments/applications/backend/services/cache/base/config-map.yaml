apiVersion: v1
kind: ConfigMap
metadata:
  name: cache-config
  namespace: "go-visitors-backend-aws"
data:
  redis.conf: |
    # Redis configuration file
    bind 0.0.0.0
    protected-mode no
    # Other configurations
